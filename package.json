{
  "name": "authentification-f1",
  "version": "1.0.0",
  "description": "Authentification workshop",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"  cd server && npm run dev  \"       \"   cd client && npm start   \"  "
  },
  "author": "Hannachi Mehdi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
